import { useVisibility } from "@/contexts/visibility";
import { nativeApi } from "@/util/native";
import { ScreenshotRect } from "electron/preload";
import { useEffect, useState } from "react";

export default function ScreenshotOverlay() {
  
}